# Base URL of the FastAPI backend. Defaults to http://localhost:8001.
# (Port 8000 is occupied by the Hakuryu backend Docker container.)
# Copy to .env and adjust if your backend runs on another port.
VITE_API_URL=http://localhost:8001
